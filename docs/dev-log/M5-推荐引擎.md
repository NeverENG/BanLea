# M5 · 推荐引擎（阶段记录）
> 日期：2026-06-09 · 对应计划 §6 / §13 M5
> 状态：已开始。M4 已提供 domain 快照、书单与看板独立入口，推荐流可以复用现有画像、证据和书单数据。

## 本阶段目标（计划 M5）
onboarding 种子建档（冷启动）+ 猜你想学/想看候选生成 + 本地排序 + 点击/停留双更新（画像 + 算法），形成入口二的推荐信息流基础。

## 起点
- 已有 `types/recommendation`：
  - `learn / read` 两类候选；
  - `interest_match / adjacency / mentioned / difficulty_fit / novelty` 五类排序特征；
  - 候选包含 `score / shownAt / clicked / dwellSeconds / skipped` 反馈字段。
- 已有推荐行为 evidence：
  - `recordRecommendationClick`；
  - `recordRecommendationSkip`；
  - 强推荐反馈会触发画像重评估。
- 已有 `reading_list`：
  - tutor 资源建议可入书单；
  - 书单状态可写回 reading evidence。
- `core/recommender` 目前为空入口，需要先补纯逻辑排序与反馈骨架。

## 优先增量
1. 本地排序器：输入候选、特征权重和探索参数，输出稳定排序与分数。
2. 反馈学习：点击、停留、跳过更新本地权重，避免只更新画像不更新排序。
3. 冷启动种子：onboarding 回答转为画像种子和首批方向。
4. 信息流视图：逐条展示猜你想学/想看，点击/跳过/停留写回 evidence。

## 已完成增量一：本地排序器骨架
- 实现 `core/recommender`：
  - 新增默认特征权重 `DEFAULT_RECOMMENDER_WEIGHTS`；
  - 新增 `scoreRecommendation`，按推荐特征计算候选分数；
  - 新增 `rankRecommendations`，输出稳定排序和 `rank`；
  - 支持 `noveltyBoost`，为探索项提供显式加权。
- 新增单测：
  - 验证默认权重分数；
  - 验证排序和 rank 写入；
  - 验证同分稳定排序；
  - 验证探索加权可以提升新鲜候选。

## 已完成增量二：反馈学习骨架
- 扩展 `core/recommender`：
  - 新增 `updateRecommendationWeights`；
  - 点击会按候选活跃特征提高权重；
  - 长停留点击比普通点击提供更强正反馈；
  - 跳过会按候选活跃特征降低权重；
  - 权重更新带上下界，避免反馈无限放大。
- 新增单测：
  - 验证点击增权；
  - 验证长停留更强；
  - 验证跳过降权；
  - 验证权重 clamp。

## 已完成增量三：候选输入建模
- 扩展 `core/recommender`：
  - 新增主题种子 `RecommendationTopicSeed`；
  - 新增书单种子 `RecommendationReadingSeed`；
  - 新增 `buildRecommendationCandidates`；
  - 支持从画像下一步、画像缺口、近期提问和手工种子生成 `learn` 候选；
  - 支持从未完成书单项生成 `read` 候选；
  - 过滤空种子和已读书单项，按 `kind + topic` 去重并合并特征；
  - 输出复用本地 ranker 的分数排序和 limit 截断。
- 新增单测：
  - 验证主题种子生成学习候选；
  - 验证未完成书单生成阅读候选；
  - 验证空项过滤、已读过滤和去重合并；
  - 验证输出按分数排序且 obey limit。

## 已完成增量四：feed 视图模型
- 新增 `features/feed`：
  - 新增 `buildFeedRecommendationView`；
  - 从 domain 快照提取画像 `nextFocus` 和 `gaps / misconceptions` 作为学习候选种子；
  - 从最近用户提问提取学习候选种子；
  - 从未完成书单项提取阅读候选种子；
  - 复用 `buildRecommendationCandidates` 输出可展示的信息流 item；
  - 提供来源计数与空状态原因。
- 新增单测：
  - 验证画像、近期提问、书单可生成 feed items；
  - 验证忽略 assistant 消息和已读书单项；
  - 验证 recent message limit 与输出 limit。

## 已完成增量五：推荐信息流主视图入口
- 新增 `features/feed/FeedWorkspaceView`：
  - 展示推荐候选数、主题种子数和书单种子数；
  - 展示 `learn / read` 信息流条目、推荐理由、分数和主要特征；
  - 提供刷新入口与空状态。
- 更新首页：
  - 主工作区新增“推荐”标签；
  - 缓存最近一次 `DomainLearningSnapshot`；
  - 使用 `buildFeedRecommendationView` 从同一份 domain 快照派生推荐信息流。

## 已完成增量六：ranker 权重仓储
- 新增 `db/rankerWeightRepo`：
  - 读取 `ranker_weights` 表；
  - 过滤未知推荐特征；
  - 输出 `RecoFeatureKey -> weight` 映射；
  - 批量 upsert 已知特征权重。
- 更新 DB 入口：
  - 导出 `createRankerWeightRepository`；
  - 新增 `getRankerWeightRepository`。
- 新增单测：
  - 验证读取与未知特征过滤；
  - 验证权重映射；
  - 验证 upsert SQL 和写入参数。

## 已完成增量七：feed 反馈服务
- 扩展 `features/feed`：
  - 新增 `recordFeedRecommendationFeedback`；
  - 点击推荐写入 `recordRecommendationClick` evidence；
  - 跳过推荐写入 `recordRecommendationSkip` evidence；
  - 读取当前 ranker 权重后调用 `updateRecommendationWeights`；
  - 将更新后的权重写回 `ranker_weights`。
- 新增单测：
  - 验证点击路径写 evidence 并提升活跃特征权重；
  - 验证跳过路径写 evidence 并降低活跃特征权重。

## 已完成增量八：feed 反馈主视图交互
- 扩展 `FeedWorkspaceView`：
  - 每条推荐显示“已看 / 跳过”操作；
  - 反馈写入时禁用当前条目按钮；
  - 复用页面刷新状态禁用反馈按钮。
- 更新首页：
  - 新增 feed 反馈状态；
  - 点击“已看”调用 feed 反馈服务，写入推荐点击 evidence 并更新 ranker 权重；
  - 点击“跳过”调用 feed 反馈服务，写入推荐跳过 evidence 并更新 ranker 权重；
  - 反馈完成后刷新 domain 快照并更新最近证据/闭环状态。

## 已完成增量九：推荐候选持久化仓储
- 扩展推荐类型：
  - `Recommendation` 新增 `domain` 字段；
  - 默认 domain 为 `global`，保持旧候选解析兼容。
- 新增迁移：
  - `0002_recommendation_domain.sql` 为 `recommendations` 增加 `domain_id`；
  - 新增按 domain 与 score 查询的索引。
- 新增 `db/recommendationRepo`：
  - 插入推荐候选并返回 recommendation id；
  - 按 domain 和 score 读取候选；
  - 支持 `markShown / markClicked / markSkipped` 更新反馈字段。
- 更新 DB 入口：
  - 导出 `createRecommendationRepository`；
  - 新增 `getRecommendationRepository`。
- 新增单测：
  - 验证 domain 默认值；
  - 验证推荐插入、读取和反馈字段更新。

## 已完成增量十：feed 候选写库与 recommendation id 关联
- 扩展 `db/recommendationRepo`：
  - 新增按 `domain + kind + topic` 查找候选；
  - 新增 `upsertCandidate`，重复候选更新理由、特征和分数；
  - 已存在候选更新时保留 `shownAt / clicked / dwellSeconds / skipped` 反馈状态。
- 扩展 `features/feed`：
  - `FeedRecommendationItem` 新增 `recommendationId`；
  - 新增 `persistFeedRecommendationView`，将 feed 候选写入 `recommendations`；
  - 写入后调用 `markShown`，并把返回的 recommendation id 回填给 UI；
  - feed 点击/跳过反馈会携带真实 recommendation id 写入 evidence。
- 更新首页：
  - 推荐视图从纯 `useMemo` 派生改为 state；
  - domain 快照更新后先生成本地 feed，再异步持久化候选；
  - 持久化完成后刷新 feed view 中的 recommendation id 和同步状态。
- 新增单测：
  - 验证 `upsertCandidate` 更新已存在候选并保留反馈状态；
  - 验证 feed 反馈会透传 recommendation id；
  - 验证 feed view 持久化会 upsert 候选、标记 shown 并回填 id。

## 已完成增量十一：feed 读取学习权重排序
- 扩展 `features/feed`：
  - `buildFeedRecommendationView` 新增 `weights` 输入；
  - 生成 feed 候选时将权重传入 `buildRecommendationCandidates`；
  - 保持默认权重路径兼容，未传权重时仍使用 core ranker 默认值。
- 更新首页：
  - domain 快照变化后先读取 `ranker_weights`；
  - 使用学习后的权重生成 feed 排序；
  - 再将排序后的候选写入 `recommendations` 并回填 recommendation id。
- 新增单测：
  - 验证传入学习权重后，feed item 排序会随权重变化。

## 验证
- `npm run typecheck` 通过。
- `npm test` 通过：24 个测试文件，134 个用例。
- `npm run build` 通过。
- `git diff --check` 通过。

## 下一步
继续 M5：补 onboarding 冷启动种子，把用户初始兴趣/目标转成首批推荐输入。
