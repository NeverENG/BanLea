# M4 · 书单与看板（阶段记录）
> 日期：2026-06-09 · 对应计划 §13 M4
> 状态：已开始。M3 已把 tutor 资源建议接入 `reading_list`，并能通过书单状态变更写回 reading evidence。

## 本阶段目标（计划 M4）
待读书单（分类 / 已读 / 停留）+ 数据看板，让入口二的“书单与学习状态”可独立查看，并为后续推荐流提供稳定数据基础。

## 起点
- 已有 `reading_list` 表与 `ReadingListRepository`：
  - 插入 tutor 资源建议；
  - 按 domain 查询；
  - 更新状态并写回 reading evidence。
- 已有 evidence / portrait timeline：
  - 可展示当前 domain 最近证据；
  - 可展示画像版本演化。
- 当前首页右栏已有嵌入式书单，但还不是独立 M4 视图。

## 优先增量
1. 书单统计摘要：按 domain 汇总 todo / later / done / reading 数量和已读停留时长。
2. 书单视图模型：输出可直接给 UI 渲染的分组列表。
3. 看板摘要：基于 evidence、reading_list、portrait timeline 生成学习状态指标。
4. 首页右栏降重：保留主入口必要信息，把完整书单/看板能力下沉到 feature 层。

## 已完成增量一：书单统计摘要
- 扩展 `features/reading-list`：
  - 新增 `ReadingListSummary`；
  - 新增 `summarizeReadingList`，按 `todo / reading / done / later` 汇总数量；
  - 统计 `doneDwellSeconds`，只累计已读项停留时长；
  - 新增 `loadReadingListOverview`，一次读取列表与摘要。
- 更新首页右栏：
  - 待读书单区域显示全部、待读、稍后、已读和已读停留秒数；
  - 原列表和状态操作保留。
- 新增单测：
  - 验证状态计数；
  - 验证已读停留时长只累计 done 项；
  - 验证 overview 一次返回列表和摘要。

## 已完成增量二：书单分组视图模型
- 扩展 `features/reading-list`：
  - 新增 `ReadingListGroup`；
  - 新增 `groupReadingListItems`；
  - 按稳定顺序输出 `todo / reading / later / done` 分组；
  - `loadReadingListOverview` 同时返回 items、groups、summary。
- 更新首页右栏：
  - 待读书单改为按状态分组展示；
  - 每组显示状态标签和条目数；
  - 每组暂时显示前 3 条，避免右栏过长。
- 新增单测：
  - 验证分组顺序；
  - 验证 overview 返回分组数据。

## 已完成增量三：看板摘要
- 新增 `features/dashboard`：
  - `buildLearningDashboardSummary` 汇总 reading、evidence、portrait timeline 和 loop status；
  - 输出资料总数、已读数、稍后数、已读停留时长；
  - 输出证据总数、待消费/已消费证据数；
  - 输出最新画像版本、可信度、画像版本数和最近活动时间。
- 更新首页右栏：
  - 新增“看板摘要”区域；
  - 与书单、证据、画像状态使用同一 domain 快照刷新。
- 新增单测：
  - 验证书单、证据、画像指标汇总正确。

## 已完成增量四：右栏视图拆分
- 新增 `features/dashboard/DashboardSummaryPanel`：
  - 承接首页右栏的看板摘要展示；
  - 保持展示字段不变。
- 新增 `features/reading-list/ReadingListPanel`：
  - 承接书单摘要、分组列表和状态操作按钮；
  - App 只传入数据和状态变更回调。
- 更新首页：
  - 删除右栏中大段内联看板/书单 JSX；
  - 保持刷新、状态变更和证据回流逻辑不变。

## 已完成增量五：证据与画像面板拆分
- 新增 `features/evidence/EvidenceStatusPanel`：
  - 承接最近证据、证据消费列表和闭环状态展示；
  - 将 evidence/loop 展示文案从 `App.tsx` 移出。
- 新增 `features/portrait/PortraitStatusPanel`：
  - 承接画像状态、触发判断、刷新按钮和版本演化展示；
  - 将触发原因标签和画像摘要逻辑从 `App.tsx` 移出。
- 更新首页：
  - 右栏底部改为组合 feature 面板；
  - App 继续只负责数据加载和刷新回调。

## 已完成增量六：domain 快照加载服务
- 扩展 `features/dashboard`：
  - 新增 `DomainLearningSnapshot` 与 `loadDomainLearningSnapshot`；
  - 统一编排 loop status、portrait timeline、evidence timeline、reading list overview、tutor history；
  - 在 feature 层生成 dashboard summary，避免首页重复拼装。
- 新增 `features/dashboard/runtimeSnapshot`：
  - 封装本地 evidence / portrait / reading list / tutor session 仓库获取；
  - 首页只调用 `loadRuntimeDomainLearningSnapshot(domain)`。
- 更新首页：
  - 删除内联 `loadDomainLoopSnapshot`；
  - 新增 `applyDomainLearningSnapshot`，集中把快照写回 React state。
- 新增单测：
  - 验证快照服务能从仓库依赖加载完整 domain 看板数据；
  - 覆盖书单摘要、证据 timeline、画像 timeline、历史消息和 dashboard summary。

## 已完成增量七：书单独立主视图入口
- 新增 `features/reading-list/ReadingListWorkspaceView`：
  - 主区域展示完整分组书单，不再限制为右栏前三条；
  - 展示全部、待读、阅读中、已读、稍后和已读停留秒数；
  - 支持在完整视图中切换 `todo / reading / done / later` 状态。
- 更新首页：
  - 主工作区新增“辅导 / 书单”视图切换；
  - 默认仍进入提问式辅导；
  - 切到书单后显示 M4 书单与学习状态入口。

## 已完成增量八：看板独立主视图入口
- 新增 `features/dashboard/DashboardWorkspaceView`：
  - 主区域展示资料、证据、画像、停留四类核心指标；
  - 展示画像触发状态、未消费证据和刷新入口；
  - 展示证据流与画像演化列表，形成可扫描的学习状态页。
- 更新首页：
  - 主工作区视图切换扩展为“辅导 / 书单 / 看板”；
  - 使用统一视图元数据管理标题；
  - 看板页复用同一份 domain 快照数据。

## 已完成增量九：书单主视图刷新入口
- 扩展 `ReadingListWorkspaceView`：
  - 显示快照读取中状态；
  - 增加独立刷新按钮；
  - 刷新复用首页同一份 domain 快照加载服务。
- 更新首页：
  - 书单主视图不再依赖右栏刷新入口。

## 验证
- `npm run typecheck` 通过。
- `npm test` 通过：22 个测试文件，108 个用例。
- `npm run build` 通过。
- `git diff --check` 通过。

## 下一步
继续完善 M4：评估 M4 最小闭环是否可标记完成，并补齐完成说明。
